diff --git a/arch/arm/Kconfig b/arch/arm/Kconfig
index 1234567..abcdefg 100644
--- a/arch/arm/Kconfig
+++ b/arch/arm/Kconfig
@@ -1560,7 +1560,8 @@ config THUMB2_KERNEL
 	bool "Compile the kernel in Thumb-2 mode"
 	depends on CPU_V7 && !CPU_V6 && !CPU_V6K
 	default n
-	select AEABI
+	# PostmarketOS: Disable Thumb-2 for compatibility
+	depends on n
 	select CC_STACKPROTECTOR if !ARM_UNWIND
 	help
 	  By default, the kernel is compiled in ARM mode. You can compile
diff --git a/arch/arm/Makefile b/arch/arm/Makefile
index 1234567..abcdefg 100644
--- a/arch/arm/Makefile
+++ b/arch/arm/Makefile
@@ -18,6 +18,9 @@ LDFLAGS_vmlinux	:=-p --no-undefined -X
 ifeq ($(CONFIG_CPU_ENDIAN_BE8),y)
 LDFLAGS_vmlinux	+= --be8
 endif
+
+# PostmarketOS: Force ARM mode, disable Thumb
+CFLAGS_KERNEL += -marm -mno-thumb-interwork
 
 OBJCOPYFLAGS	:=-O binary -R .comment -S
 GZFLAGS		:=-9
diff --git a/arch/arm/boot/compressed/Makefile b/arch/arm/boot/compressed/Makefile
index 1234567..abcdefg 100644
--- a/arch/arm/boot/compressed/Makefile
+++ b/arch/arm/boot/compressed/Makefile
@@ -121,6 +121,9 @@ endif
 CFLAGS_fdt_rw.o := $(nossp_flags)
 CFLAGS_fdt_wip.o := $(nossp_flags)
 
+# PostmarketOS: Force ARM mode for compressed kernel
+CFLAGS_KERNEL += -marm -mno-thumb-interwork
+
 ccflags-y := -fpic -mno-single-pic-base -fno-builtin -I$(obj)
 asflags-y := -DZIMAGE
 
diff --git a/include/linux/linkage.h b/include/linux/linkage.h
index 1234567..abcdefg 100644
--- a/include/linux/linkage.h
+++ b/include/linux/linkage.h
@@ -12,6 +12,11 @@
 #define __ALIGN_STR	".align 4,0x90"
 #endif
 
+/* PostmarketOS: Force ARM mode in assembly */
+#ifdef __ASSEMBLY__
+.arm
+#endif
+
 #ifdef __ASSEMBLY__
 
 #ifndef LINKER_SCRIPT
