diff --git a/arch/arm/Kconfig b/arch/arm/Kconfig
index 1234567..abcdefg 100644
--- a/arch/arm/Kconfig
+++ b/arch/arm/Kconfig
@@ -1560,11 +1560,12 @@ config ARM_THUMB
 config THUMB2_KERNEL
 	bool "Compile the kernel in Thumb-2 mode"
 	depends on CPU_V7 && !CPU_V6 && !CPU_V6K
-	default n
-	select AEABI
+	# PostmarketOS: Completely disable Thumb-2 for stability
+	depends on n
+	default n 
 	select CC_STACKPROTECTOR if !ARM_UNWIND
 	help
-	  By default, the kernel is compiled in ARM mode. You can compile
+	  PostmarketOS: Thumb-2 disabled - By default, the kernel is compiled in ARM mode. You can compile
 	  the kernel in Thumb-2 mode by enabling this option. Since
 	  Thumb-2 instructions are 16-bit wide except for load, store,
 	  and control flow instructions, this option can reduce the size
@@ -1577,6 +1578,11 @@ config THUMB2_KERNEL
 
 	  If unsure, say N.
 
+# PostmarketOS: Force ARM mode
+config ARM_MODE_FORCED
+	bool
+	default y
+
 config ARM_ASM_UNIFIED
 	bool
 
diff --git a/arch/arm/Makefile b/arch/arm/Makefile
index 1234567..abcdefg 100644
--- a/arch/arm/Makefile
+++ b/arch/arm/Makefile
@@ -18,6 +18,12 @@ LDFLAGS_vmlinux	:=-p --no-undefined -X
 ifeq ($(CONFIG_CPU_ENDIAN_BE8),y)
 LDFLAGS_vmlinux	+= --be8
 endif
+
+# PostmarketOS: Comprehensive ARM mode forcing
+CFLAGS_KERNEL += -marm -mno-thumb-interwork
+AFLAGS_KERNEL += -marm -Wa,-mno-warn-deprecated
+CFLAGS_MODULE += -marm -mno-thumb-interwork
+AFLAGS_MODULE += -marm
 
 OBJCOPYFLAGS	:=-O binary -R .comment -S
 GZFLAGS		:=-9
@@ -25,6 +31,9 @@ GZFLAGS		:=-9
 # Explicitly specifiy 32-bit ARM ISA since toolchain default can be -mthumb:
 KBUILD_CFLAGS	+=$(call cc-option,-marm,)
 
+# PostmarketOS: Additional ARM mode enforcement
+KBUILD_CFLAGS += -mno-thumb-interwork -marm
+KBUILD_AFLAGS += -marm -Wa,-mno-warn-deprecated
 # Prevent redefinition of __LINUX_ARM_ARCH__
 ifeq ($(filter -D__LINUX_ARM_ARCH__%,$(KBUILD_CFLAGS)),)
 KBUILD_CFLAGS += -D__LINUX_ARM_ARCH__=$(arch-y)
@@ -108,6 +117,10 @@ ifeq ($(CONFIG_ARM_UNWIND),y)
 CFLAGS_ABI	+=-funwind-tables
 endif
 
+# PostmarketOS: Override any Thumb settings
+CFLAGS_ABI := $(filter-out -mthumb,$(CFLAGS_ABI))
+CFLAGS_ABI += -marm
+
 ifeq ($(CONFIG_THUMB2_KERNEL),y)
 AFLAGS_AUTOIT	:=$(call as-option,-Wa$(comma)-mimplicit-it=always,-Wa$(comma)-mauto-it)
 AFLAGS_NOWARN	:=$(call as-option,-Wa$(comma)-mno-warn-deprecated,-Wa$(comma)-W)
@@ -115,6 +128,11 @@ CFLAGS_ISA	:=-mthumb $(AFLAGS_AUTOIT) $(AFLAGS_NOWARN)
 AFLAGS_ISA	:=$(CFLAGS_ISA) -Wa$(comma)-mthumb
 # Work around buggy relocation from gas if requested:
 ifeq ($(CONFIG_THUMB2_AVOID_R_ARM_THM_JUMP11),y)
+# PostmarketOS: Force disable this problematic option
+else
+# PostmarketOS: Force ARM mode instead of Thumb-2
+CFLAGS_ISA := -marm -mno-thumb-interwork
+AFLAGS_ISA := -marm -Wa,-mno-warn-deprecated
 CFLAGS_MODULE	+=-fno-optimize-sibling-calls
 endif
 else
diff --git a/arch/arm/boot/compressed/Makefile b/arch/arm/boot/compressed/Makefile
index 1234567..abcdefg 100644
--- a/arch/arm/boot/compressed/Makefile
+++ b/arch/arm/boot/compressed/Makefile
@@ -121,12 +121,17 @@ endif
 CFLAGS_fdt_rw.o := $(nossp_flags)
 CFLAGS_fdt_wip.o := $(nossp_flags)
 
+# PostmarketOS: Force ARM mode for compressed kernel
+CFLAGS_KERNEL += -marm -mno-thumb-interwork
+AFLAGS_KERNEL += -marm -Wa,-mno-warn-deprecated
+
 ccflags-y := -fpic -mno-single-pic-base -fno-builtin -I$(obj)
 asflags-y := -DZIMAGE
 
 # Supply kernel BSS size to the decompressor via a linker symbol.
 KBSS_SZ = $(shell $(CROSS_COMPILE)size $(obj)/../../../../vmlinux | \
 		awk 'END{print $$3}')
+		
 LDFLAGS_vmlinux += --defsym _kernel_bss_size=$(KBSS_SZ)
 # Supply ZRELADDR to the decompressor via a linker symbol.
 ifneq ($(CONFIG_AUTO_ZRELADDR),y)
@@ -136,6 +141,10 @@ LDFLAGS_vmlinux += --defsym zreladdr=$(ZRELADDR)
 ifeq ($(CONFIG_CPU_ENDIAN_BE8),y)
 LDFLAGS_vmlinux += --be8
 endif
+
+# PostmarketOS: Force ARM mode in compressed boot
+LDFLAGS_vmlinux += --force-arm-mode
+
 # ?
 LDFLAGS_vmlinux += -p
 # Report unresolved symbol references
diff --git a/include/linux/linkage.h b/include/linux/linkage.h
index 1234567..abcdefg 100644
--- a/include/linux/linkage.h
+++ b/include/linux/linkage.h
@@ -12,6 +12,16 @@
 #define __ALIGN_STR	".align 4,0x90"
 #endif
 
+/* PostmarketOS: Comprehensive ARM mode forcing */
+#ifdef __ASSEMBLY__
+.arm
+.syntax unified
+
+/* Disable Thumb-2 completely */
+#ifdef CONFIG_THUMB2_KERNEL
+#error "Thumb-2 kernel disabled by PostmarketOS patches"
+#endif
+#endif
 #ifdef __ASSEMBLY__
 
 #ifndef LINKER_SCRIPT
