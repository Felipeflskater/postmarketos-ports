diff --git a/include/linux/const.h b/include/linux/const.h
index 1234567..abcdefg 100644
--- a/include/linux/const.h
+++ b/include/linux/const.h
@@ -21,4 +21,9 @@
 #define _AT(T,X)	((T)(X))
 #endif
 
+#ifndef __AC
+#define __AC(X,Y)	(X##Y)
+#define _AC(X,Y)	__AC(X,Y)
+#endif
+
 #endif /* !(_LINUX_CONST_H) */
diff --git a/arch/arm/include/asm/memory.h b/arch/arm/include/asm/memory.h
index 1234567..abcdefg 100644
--- a/arch/arm/include/asm/memory.h
+++ b/arch/arm/include/asm/memory.h
@@ -15,6 +15,15 @@
 #include <linux/compiler.h>
 #include <linux/const.h>
 #include <linux/types.h>
+
+/* GCC 15+ compatibility - UL macro fix */
+#ifndef __ASSEMBLY__
+#ifndef UL
+#define UL(x) _AC(x, UL)
+#endif
+#else
+#define UL(x) (x)
+#endif
 #include <asm/sizes.h>
 
 /*
@@ -167,8 +176,8 @@ static inline unsigned long __phys_to_virt(unsigned long x)
 /*
  * PFNs are used to describe any physical page; this means
  * PFN 0 == physical address 0.
- */
-#define PAGE_OFFSET		UL(CONFIG_PAGE_OFFSET)
-#define PHYS_OFFSET		UL(CONFIG_PHYS_OFFSET)
+#define PAGE_OFFSET             UL(0xC0000000)
+#define PHYS_OFFSET             UL(0x00000000)
 
 /*
  * The module can be at any place in ram in nommu mode.
diff --git a/arch/arm/Makefile b/arch/arm/Makefile
index 1234567..abcdefg 100644
--- a/arch/arm/Makefile
+++ b/arch/arm/Makefile
@@ -20,7 +20,10 @@ GZFLAGS		:=-9
 # Explicitly specifiy 32-bit ARM ISA since toolchain default can be -mthumb:
 KBUILD_CFLAGS	+=$(call cc-option,-marm,)
 
-KBUILD_CFLAGS	+= -D__LINUX_ARM_ARCH__=$(arch-y)
+# Prevent redefinition of __LINUX_ARM_ARCH__
+ifeq ($(filter -D__LINUX_ARM_ARCH__%,$(KBUILD_CFLAGS)),)
+KBUILD_CFLAGS += -D__LINUX_ARM_ARCH__=$(arch-y)
+endif
 KBUILD_AFLAGS	+= -D__LINUX_ARM_ARCH__=$(arch-y)
 
 comma = ,
